POSTGRES_DB=rag
POSTGRES_CLUSTER_ADMIN_PASSWORD=
POSTGRES_PORT=5432
RUSTFS_API_PORT=9000
RUSTFS_CONSOLE_PORT=9001
RUSTFS_ROOT_ACCESS_KEY=
RUSTFS_ROOT_SECRET_KEY=
# Set to 1 only when PostgreSQL and RustFS should remain running after Ctrl+C.
RAG_DEV_KEEP_DEPENDENCIES=0
